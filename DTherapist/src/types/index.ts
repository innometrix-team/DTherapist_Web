
export interface Service {
    title: string;
    imageUrl: string;
    description: string;
  }
  
  
  export interface Step {
    icon: React.ReactNode;
    title: string;
    desc: string;
  }
  